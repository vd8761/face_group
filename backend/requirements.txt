# Core framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1
pydantic[email]

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Auth
python-jose[cryptography]==3.3.0
bcrypt==4.2.1

# Storage (Cloudflare R2 — S3-compatible)
boto3==1.35.76
Pillow==11.0.0

# ML — Face Detection + Embedding
# buffalo_sc model: ~85MB download, ~280MB RAM at runtime (fits 512MB free tier)
# buffalo_l was replaced because it uses 1-2GB RAM (OOM on free tier)
insightface==0.7.3
onnxruntime==1.20.1
numpy==1.26.4
opencv-python-headless==4.10.0.84

# Clustering
hdbscan>=0.8.38
scipy>=1.11.0

# HTTP utilities
python-multipart==0.0.17
httpx==0.28.1

# Observability
sentry-sdk[fastapi]==2.19.0
