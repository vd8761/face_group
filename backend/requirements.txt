# Core framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1
pydantic[email]

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0               # Async PostgreSQL driver for Neon DB
alembic==1.14.0

# Auth
python-jose[cryptography]==3.3.0
bcrypt==4.2.1

# Storage (Cloudflare R2 — S3-compatible)
boto3==1.35.76
Pillow==11.0.0                 # Thumbnail generation

# ML — Face Detection + Embedding
# face_recognition uses dlib HOG-based detector — only ~120MB RAM (fits free tier)
# InsightFace was replaced because it requires 1-2GB RAM (OOM on 512MB Render free tier)
face_recognition==1.3.0
numpy==1.26.4

# Clustering
hdbscan>=0.8.38
scipy>=1.11.0

# HTTP utilities
python-multipart==0.0.17      # File upload support
httpx==0.28.1

# Observability (optional but recommended for Render)
sentry-sdk[fastapi]==2.19.0
