# Core framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1
pydantic[email]

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Auth
python-jose[cryptography]==3.3.0
bcrypt==4.2.1

# Storage (Cloudflare R2 — S3-compatible)
boto3==1.35.76
Pillow==11.0.0

# Image format support
rawpy>=0.19.0          # Sony ARW, Canon CR2/CR3, Nikon NEF, DNG, RAF, ORF etc.
pillow-heif>=0.15.0    # HEIC / HEIF (iPhone photos)

# ML — Face Detection + Embedding
# buffalo_l model: ~500MB download, ~1.5GB RAM at runtime
# Requires Render Standard plan (2GB RAM) or equivalent
insightface==0.7.3
# The GPU wheel also contains the CPU execution provider, so Linux/Windows
# deployments can use CUDA when present and fall back cleanly when it is not.
onnxruntime==1.20.1; platform_system == "Darwin"
onnxruntime-gpu[cuda,cudnn]==1.21.0; platform_system != "Darwin"
numpy==1.26.4
opencv-python-headless==4.10.0.84

# Clustering
scikit-learn>=1.3.0
scipy>=1.11.0

# HTTP utilities
python-multipart==0.0.17
httpx==0.28.1

# Observability
sentry-sdk[fastapi]==2.19.0
psutil>=5.9.8
nvidia-ml-py>=12.535.133
celery==5.4.0  
redis==5.0.4  
